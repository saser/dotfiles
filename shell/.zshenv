# Environment variables.

# Tell where the root of my dotfiles are.
export DOTS_HOME=${HOME}/dotfiles

# Needed to make sure zplug works.
export ZPLUG_HOME=${HOME}/.zplug

# I use neovim as my main editor.
export EDITOR=nvim
