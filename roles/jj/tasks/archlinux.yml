- name: Install jj and related programs (Arch Linux)
  become: true
  community.general.pacman:
    name:
      - jujutsu

- name: Install jj and related programs (from AUR) (Arch Linux)
  become: true
  become_user: '{{ arch_user }}'
  kewlfft.aur.aur:
    name:
      - jjui
    use: yay
