#!/usr/bin/env zsh

{% for var in _variables %}
{% if var.disable_shell_expansion is defined and var.disable_shell_expansion == true %}
export {{ var.name }}='{{ var.value }}'
{% else %}
export {{ var.name }}="{{ var.value }}"
{% endif %}
{% endfor %}
