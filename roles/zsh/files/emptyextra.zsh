# This file is empty but will be placed in the zsh extras directory. It exists
# only to paper over an issue where no role has installed a zshextra file, and
# zsh complains about no files matching a glob when zsh starts up and tries to
# source zsh extra files.
